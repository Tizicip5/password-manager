
/*
#ifndef PROJECT_MAIN_H
#define PROJECT_MAIN_H
#include <string>

class Main {
public:
    void run();
private:
    bool hasloByloWykarzystane(const std::string& basicString, const std::vector<Haslo>& vector);
    bool sprawdzenieHasla(std::string& basicString);
    vector<Haslo> wyszykiwanieHasla(const vector<Haslo>& hasla, const std::string& slowoKluczowe);
    void wyswietlenieHasla(const vector<Haslo>& has);
    void dodawanieHasla(vector<Haslo>& haslo);
    std::string szyfrowanieHasla(std::string& haslo, std::string klucz);
    std::string odszyfrowanieHasla(std::string szyfrowanieHasla, std::string kluczSzyfrowania);
    void zapisywanieHaslaDoPliku(const Haslo& haslo, const std::string& nazwaPliku, const std::string& kluczSzyfrowania);
    std::vector<Haslo> odczytHasla(const std::string& nazwaPliku, const std::string& kluczSzyfrowania);
    void edycjaHasla(std::vector<Haslo>& hasla);
    void odczytHaslaZPliku(std::vector<Haslo>& hasla, const std::string& nazwaPliku, const std::string& kluczSzyfrowania);
    void sortowanieHaslaImie(std::vector<Haslo>& hasla);
    void sortowanieHaslaKategoria(std::vector<Haslo>& hasla);
    void dodawanieKategorii(std::vector<std::string>& kategorii, std::string nowaKategoria);
    void usuwanieKategorii(std::vector<Haslo>& hasla, std::vector<std::string>& kategorii, std::string usuwanieKategorii);
    void usuwanieHasla(std::vector<Haslo>& hasla);

};
#endif //PROJECT_MAIN_H

*/